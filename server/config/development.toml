[server]
host = "127.0.0.1"
port = 3000

[database]
driver = "mysql"
url = "mysql://root:password@localhost/money_notes_dev"
max_connections = 5

[jwt]
secret = "dev-secret-key-not-for-production"
access_token_expires = 3600
refresh_token_expires = 604800
